# Alternative GNU Make workspace makefile autogenerated by Premake

ifndef config
  config=debug
endif

ifndef verbose
  SILENT = @
endif

ifeq ($(config),debug)
  ImGui_config = debug
  GLFW_config = debug
  Walnut_config = debug
  RayTracing_config = debug

else ifeq ($(config),release)
  ImGui_config = release
  GLFW_config = release
  Walnut_config = release
  RayTracing_config = release

else ifeq ($(config),dist)
  ImGui_config = dist
  GLFW_config = dist
  Walnut_config = dist
  RayTracing_config = dist

else
  $(error "invalid configuration $(config)")
endif

PROJECTS := ImGui GLFW Walnut RayTracing

.PHONY: all clean help $(PROJECTS) Core Dependencies

all: $(PROJECTS)

Core: Walnut

Dependencies: GLFW ImGui

ImGui:
ifneq (,$(ImGui_config))
	@echo "==== Building ImGui ($(ImGui_config)) ===="
	@${MAKE} --no-print-directory -C vendor/imgui -f Makefile config=$(ImGui_config)
endif

GLFW:
ifneq (,$(GLFW_config))
	@echo "==== Building GLFW ($(GLFW_config)) ===="
	@${MAKE} --no-print-directory -C vendor/GLFW -f Makefile config=$(GLFW_config)
endif

Walnut: ImGui GLFW
ifneq (,$(Walnut_config))
	@echo "==== Building Walnut ($(Walnut_config)) ===="
	@${MAKE} --no-print-directory -C Walnut -f Makefile config=$(Walnut_config)
endif

RayTracing: Walnut GLFW ImGui
ifneq (,$(RayTracing_config))
	@echo "==== Building RayTracing ($(RayTracing_config)) ===="
	@${MAKE} --no-print-directory -C RayTracing -f Makefile config=$(RayTracing_config)
endif

clean:
	@${MAKE} --no-print-directory -C vendor/imgui -f Makefile clean
	@${MAKE} --no-print-directory -C vendor/GLFW -f Makefile clean
	@${MAKE} --no-print-directory -C Walnut -f Makefile clean
	@${MAKE} --no-print-directory -C RayTracing -f Makefile clean

help:
	@echo "Usage: make [config=name] [target]"
	@echo ""
	@echo "CONFIGURATIONS:"
	@echo "  debug"
	@echo "  release"
	@echo "  dist"
	@echo ""
	@echo "TARGETS:"
	@echo "   all (default)"
	@echo "   clean"
	@echo "   ImGui"
	@echo "   GLFW"
	@echo "   Walnut"
	@echo "   RayTracing"
	@echo ""
	@echo "For more information, see https://github.com/premake/premake-core/wiki"